# HANDOVER — 5.3.5 全库 bug 大扫除批次（含 P0 配对挂死事故）

> ⚠️ **本文档已被 Round 2 接续批取代**：批次一经五域审查后挖出 18 项新问题
> （含 2 个批次一引入的回归）并全部修复，P0 的「真完整根因」（.lnk 同步
> PowerShell 冻结）与本批遗漏的历史遗留 P1（WS 握手 4096B peek 截断 → 桥
> 全灭）见 **`docs/HANDOVER-2026-08-30-5.3.5-review-round2.md`**。本文件保留
> 作为批次一记录；§1.4 的「修复版安装包」与 §3 行动清单以 Round 2 文档为准。
> 另：批次一「用户安装了 15:31 坏包」的判断有误 —— 用户机器实证只装过 5.3.3。

**交接时间**：2026-08-30 约 17:00
**交接人**：ZCode（GLM-5.3-Flash）会话 sess_a058a4ff
**仓库**：`D:\DeepSeek Harness\dsh max\dsh_desktop`（main 分支，**全部改动未 commit**，约 46 文件修改 + 2 新测试文件）
**基线**：5.3.4 = commit 9c5c23ca（727/722/0 全绿）
**当前测试状态**：**741 用例 / 736 过 / 0 挂 / 5 skip（全绿，EXIT=0）**

---

## 0. 一页速览（接手者先读这里）

1. **本批完成**：五域并行审查 → 确认约 46 项真 bug → 全部修复 → 全量回归全绿 → 打包过一次（有 P0 缺陷）。
2. **【最高优先级】P0 未收尾**：用户实报「点开始配对没反应」→ 根因已定位（我引入的备份清理钩子同步删数 GB 备份冻结 sidecar 事件循环 + boot.start 应答被集成序列拖慢）→ **修复代码已写完并过全量测试**，但**修复版安装包尚未打包出来**（NSIS 步骤没跑完，磁盘上的 setup.exe 还是带 bug 的 15:31 版）。用户机器上仍装着坏版本。
3. **接手后第一件事**：见 §3「下一步行动清单」，按序执行，总计约 15 分钟机器时间。
4. 本文档自身就是交接物之一，已写入 `docs/HANDOVER-2026-08-30-5.3.5-batch.md`。

---

## 1. P0 事故完整记录（用户实报：5.3.5 点开始配对没反应）

### 1.1 症状时间线

- 用户安装了我打的首个 5.3.5 安装包（15:31 版），点设置→连接手机→「开始配对」→ 按钮变灰（busy）后永远不恢复，二维码不出，状态恒「未启动」。
- 约 3 分钟后弹出「DSH 服务已停止 / 退出码 boot / `sidecar call timeout (180s)`」的 died 页；点「重新启动」报「重启失败，请重试」；**安全模式重启后同样复现**（注意：用户复现时装的仍是坏版本，安全模式复现不构成对修复版的否定）。

### 1.2 排查过程（关键证据链，工具已留存）

排查工具（都在 `D:\DeepSeek Harness\dsh max\` 根目录，可复用）：
- `probe-phone-start.js` — 真实壳 + CDP：页面内查 `__DSH_BRIDGE_WS__`/`__DSH_WS_RPC__`、对 `phoneBridge.status()/start()` 做竞速超时、裸 WS 连 19873 探活、dump 壳完整 stdout/stderr 缓冲。
- `probe-rpc-methods.js` — 对活壳 19873 裸 WS 依次发 `shell.info / chrome.init / phone.status / boot.state`，鉴别「部分方法挂」vs「全挂」。

证据链：
1. 复现：release 壳 + 隔离 home → `phoneBridge.start()/status()` 12s 竞速全部 TIMEOUT；壳存活、页面存活、sidecar 存活、裸 WS 能 OPEN、`__DSH_WS_RPC__` 工厂在页面内可用（自建客户端 8s 超时被拒 = 请求发出去了没回音）。
2. `probe-rpc-methods.js`：**shell.info / chrome.init / phone.status / boot.state 全部 TIMEOUT** → 不是 phone 独有，是 sidecar RPC 面全灭。
3. 壳自带 `--bridge-test`：**4/4 过**（ping/shell.info/profile.name/plugins.removedIds 秒回）→ Rust `Sidecar::spawn` + `sc.call` 读写链健康。
4. `boot-smoke.js`（裸 sidecar + 隔离 home）：**PASS** → sidecar 单独跑完整 boot.start 健康。
5. 给 debug 壳插桩（`[dbg] call -> / reader frame / ws fwd`）后抓到铁证：**`call -> boot.start id=1` 发出后，`[dbg] reader frame` 计数 = 0** —— sidecar 的 stdout 一帧都没到 Rust，但此前的 release 运行里 `boot.web-ready` 通知明明到了并驱动了导航。
6. 读 server.ts boot.start 处理器语句序：`notify('boot.web-ready')` 在 handler 中段，其后还有一长串**同步**集成作业（余额轮询/更新调度/插件宿主/预览服务/快捷方式维护 PowerShell/junction 巡检），全部跑完才 `return`（写 RPC 应答）。→ **通知到了、应答永远不来**的不对称由此而来。
7. 用户环境放大器：我在 `guardedStartAndWait`（boot.start 关键路径内）新增的「更新备份清理钩子」是**同步** `rmSync`。用户真实 home 的 `<userData>/backups/` 累积了 5.3.2→5.3.4 三代更新的**数 GB robocopy 全量镜像**（本批之前没有任何清理方，见 §2 C1），同步删几分钟 = 事件循环冻结几分钟 = 全部 RPC 排队（配对按钮卡死）+ 180s 后 Rust 超时弹 died 页。
8. 我自己的隔离探针 home 没有备份，所以只看到 ~30-60s 的延迟（maintainShortcuts 的 PowerShell 同步作业），没有第一时间抓到分钟级阻塞 —— 这是排查走弯路的原因，**教训：用户 home 与测试 home 的数据规模差异本身就是变量**。

### 1.3 已写好的修复（代码在工作区，已过全量测试）

- **`tauri-shell/sidecar/server.ts`** boot.start 处理器：
  - `notify('boot.web-ready')` 之后**立刻 `return` 应答**；余额轮询/更新调度/插件宿主/预览服务/快捷方式/临时目录告警/junction 巡检全部挪进 `setImmediate`（应答先出、作业照跑）；`startSessionWatcher` 同样挪到 `setImmediate`。
  - 清理钩子（`confirmPreviousAgentHealthy` + `cleanupClientBackupIfHealthy`）整体挪出关键路径：`setTimeout(30_000).unref()` 延迟执行。
- **`dsh-desktop/client-updater.ts`** `cleanupClientBackupIfHealthy`：改为 **async**（`fs.promises.rm`，多 GB 删除不再占事件循环），便携 .bak/.crash 同步改异步；函数头注释写明「严禁同步删大目录」的教训。
- **`dsh-desktop/test/client-updater-cleanup.test.ts`**：适配 async 签名（3 用例全过）。
- 修复后全量回归：**741/736/0 全绿**（full-test5.log）。
- Rust 侧临时插桩（[dbg] eprintln）**已全部拆除**，`cargo check` 干净通过（中途因拆桩少个逗号报错已修）。

### 1.4 修复的验证状态（未完成项！）

- ✅ 单元/回归层：全量测试绿（741/736/0）。
- ✅ **E2E 已复验（16:4x）**：`node verify-phone-pair.cjs` 对修复版 release 壳 **PASS（P1-P10 全过）**——P2 `phoneBridge.start()` 正常返回配对 URL，配对/批准/cookie/代理/WS 透传/设置入口全部断言绿。坏版本上的 "Execution context was destroyed"（180s 超时弹 died 页导航）不再复现。
- ✅ **修复版安装包已产出**：`tauri-shell/target/release/bundle/nsis/Deepseek Harness EAC_5.3.5_x64-setup.exe`（**Aug 30 16:33，201,554,707B** —— 认准这个大小/时间；15:31 的 201,562,880B 是坏版本）。
- ✅ 修复版便携包已产出：`target/release/portable/Deepseek-Harness-EAC-5.3.5-portable.zip`（**Aug 30 16:44，240,826,039B**，SHA256 A707875E…D5484；15:34 的 240,821,961B 是坏版本）。

---

## 2. 本批已完成修复全清单（五域 ~46 项）

> 全部改动都在工作区未 commit。文件清单见 §6。已由 741 用例回归覆盖。

### 2.1 壳韧性（tauri-shell/src/main.rs，14 项）

| # | 修复 | 位置/说明 |
|---|------|-----------|
| R1 | sidecar spawn 失败分支补起 serve_ws | 原 died 页指向无人监听端口（issue #210 修复在真实故障现场复现原 bug）；listener 绑定挪到 setup 任务最前，三处 serve_ws 调用点都消费同一 listener |
| R2 | 退出兜底 kill 死代码 | `Arc::into_inner` 因槽位持有克隆恒 None；`child` 改 `AMutex<Child>`，退出时先 `*slot=None` 再轮询 try_wait 9s，超时 kill |
| R3 | sidecar 崩溃黑洞 | reader EOF 时回绝全部在途 RPC（不再挂 180s）+ 广播 `boot.server-died`（code=sidecar-exited）走 /died 链 |
| R4 | 每条 WS 连接泄漏 3 任务 | writer + shell_notify 转发 + sidecar 转发在连接结束时统一 abort |
| R5 | `call()` 泄漏 pending 表项 | 写失败/超时路径显式 `pending.remove` |
| R6 | exitAction 挂 180s | 关窗路径读配置加 2s 短超时，失败走默认 |
| R7 | 桥端口被占回退链 | `bind_ws_listener()`：19873→+25 候选→OS 分配；`ws_port()` 全局读取（15 处 URL 构造点全换）；页内 fallback 19873 与 ws-jsonrpc-client.js 的默认值一致 |
| R8 | `app.restart()` 停车 | 非主线程分支 `loop{sleep(MAX)}` 停车一个 worker → 三处改 `request_restart()` |
| R9 | DPI 混算 | 恢复位 clamp 的 min_vis/40px 兜底乘 `mscale`（wa.* 是物理像素，w/h 是逻辑像素） |
| R12 | builder min_inner_size 小屏 | 新增 `effective_min_inner()` 按主屏 work area 折算，两处 builder 使用 |
| R14 | 恢复中心/onboarding 桥注入 marker 失配 | `<meta charset="utf-8" />` 硬编码 vs 实际无斜杠写法 → quirks mode；抽 `inject_after_doctype()` 双写法匹配 + doctype 后插入兜底 |
| R15 | died URL 未编码 | boot.start 失败与 sidecar-spawn 失败的 log 参数走 `encode_query` |
| R16 | 主窗丢 disable-features | `additional_browser_args` 是整体替换语义，补回 `--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection` |
| R17 | 浮窗标签跨会话撞窗 | `sanitize_label` 后追加 FNV-1a hex8 后缀 |
| R18 | accept 循环无退避 | 错误分支 sleep 100ms |
| R13 | exit-overlay style 堆积 | `src/exit-overlay.js` 按 id 复用样式标签 |

### 2.2 安全围栏（sidecar / 壳，5 项）

| # | 修复 | 说明 |
|---|------|------|
| S1 | `files.authorize-open` 路径穿越 | `..`/大小写变体骗过字面前缀 → ShellExecuteW 开任意文件；realpath 归一化 + win32 小写折叠后比对，DANGEROUS_EXT 对归一化路径判定 |
| S2 | 预览静态服务任意文件读 | staticPort 经 chrome.init 下发页面 = 全盘读原语；`static-preview.ts` 加 `fence` 注入点（server.ts 提供：会话 cwd fileRoots + skills 根白名单，`.credentials*` 一律拒绝）+ Host 头校验（DNS rebinding 免疫）|
| S7 | `/desktop/decide\|disconnect` CSRF | 仅收 POST + 拒 `Sec-Fetch-Site: cross-site`（此前 `<img src>` 即可把配对改拒绝/踢手机）；桌面批准走 RPC 不受影响（测试本来就用 POST）|
| S8 | `ensureKernelCookie` 无超时 | 加 8s 超时（内核挂起时桥整体卡死）|
| S16 | sidecar 裸崩无兜底 | `unhandledRejection` 记日志继续跑；`uncaughtException` 记日志退场（R3 保证壳走 /died 链）|

### 2.3 手机桥（phone-bridge.ts，4 项）

| # | 修复 | 说明 |
|---|------|------|
| S4 | WS 半开悬挂 | `drop()` 里双向 `destroy()`（手机断网无 FIN 时内核侧会话永不回收）|
| S5 | 120s 空闲超时杀 SSE | 响应到达后 `setTimeout(0)` 撤销；客户端放弃有 res close 兜底 |
| S6 | rescue 路径绕过 currentWebInfo | rescue retry / recovery.reload 的 `startAndWait` 包一层：走 `guardedStartAndWait`（快照/最后良好/事故留痕）+ 成功同步 `currentWebInfo`（救援拉起后手机桥不再 503）|
| S9 | stale cookie 401 重兑重放 | 重构 `proxyOnce`；401 时仅在「本次请求实际带的是桥注入缓存 cookie」时强制重兑一次重放；手机自带 dsh-auth-* 的透传优先语义保留（有测试钉住）|
| S12 | boot.stop/重启失败不清 webUrl 缓存 | 置 `currentWebInfo = null` |

### 2.4 更新器 / 数据完整性（8 项）

| # | 修复 | 说明 |
|---|------|------|
| C1 | 备份清理承诺从未实现（两代交接虚报）| `cleanupClientBackupIfHealthy` 落地：>24h 静默删、清完回收 marker、.keep 保留语义、便携 .bak/.bak.marker/.crash 同链清理；**async + 延迟 30s**（见 §1.3 事故教训）|
| C2 | semver prerelease 误比 | 重写 `compareVersions`：按点分段、数字段数值序、数字<字母、段数少者低（旧实现 beta.2 > rc.1 / rc.1.10 < rc.1.2）|
| C3 | updater abort 只杀最后一个 | `activeProc` 单例 → `activeProcs` Set 遍历收割；plugin-updater 检测改 4 并发 worker-pool（原先 11 源 ×2 npm 进程全并发）|
| C4 | staging 互踩 | `applyBuiltinPluginUpdate` 加 singleFlight 单飞闸（并发第二个直接拒绝）；staging 保持固定路径（mock 与外部语义锚定 `stagingRoot/pkg`）|
| C5 | atomic-json 先删后写 | 两步换入：旧目标先 rename `.old-<rand>` 保底，换入失败还原，任何时刻目标路径有完整数据；签名扩 `string \| Buffer`（restore 快照逐字节保真）|
| C6 | 13 处启动关键文件裸写 | preset-sync（settings.yaml×2）、boot-server（.credentials.yaml）、plugin-guard（cordis.patch.yml 修复写/事故报告/restore copyFileSync）、builtin-collision（pkg/patch）、plugin-ops（setRemoved/setEnabled 固定 tmp 名）、market（marker）、**patch-deps（内核包 7 处）** 全部 writeFileAtomic |
| C7 | 便携 .crash 复制的是新 exe | 改从 `.bak`（真旧版）复制 |
| C8 | RESUME_INVALID 不删 .part | 该分支补删，不再空转烧完重试 |

### 2.5 其他核心层（7 项）

| # | 修复 | 说明 |
|---|------|------|
| C14 | logger 启动截断上轮日志 | 构造时 `_rollExistingOnStartup()` 先滚链再开新 |
| C15 | deepRedact getter 二次取值 | catch 里置 undefined，不再冒泡返回未脱敏对象 |
| C16 | _flush 半行裸传 | deep 模式也过 shallow masker |
| C11 | companion-sync 空表替换锚定 | 检测与替换统一 `/^\s*\[\]\s*$/m`（内联 `[]` 不再插错位致 YAML 损坏）|
| P3 | 坏 overlay 永久遮蔽资产 | `builtinPluginSourceDir` 版本不可读时回退 assets |
| C12 | session-watcher Map 泄漏 | scan 末尾清扫未枚举 key |
| C13 | ext-host 退避定时器叠挂 / started 被拒 | cancel-first；started 转移被拒（registry-write-failed 除外）时终止 Host |
| S11 | turnEndNotifyAt 慢泄漏 | 500 上限 + 淘汰最旧一半 |

### 2.6 插件资产（子智能体执行，7 文件 + raw-html 加固）

- dsh-message-rewind：watchDom 防重入（observer+interval 不再叠加）。
- dsh-font-custom：observer 模块级单例 + 重建分支读当前存储配置（不再回退旧 css）。
- dsh-eac-core-bridge：interval 幂等、inject 删未用的 sessions、工具名注册查重跳过。
- dsh-phone：`setStatus` 死三元修正（失败臂 null）、二维码脚本模块级单例（loading/loaded/failed 状态机允许重试）。
- recovery-center.html：状态读取失败文案兜底（不再显示 undefined/空串）。
- onboarding.html：busy 永卡修复（partial errors 分支复位）、Escape 提交期间不可关、noneBtn 与 pureBtn 重复按钮删除（清空全部会被后端核心回填拒绝，语义不成立）。
- dsh-agent-teams：`lib/client/index.js` 过时源码副本与 `lib/client.js` bundle 双同步（uiConversation 契约）+ 头部「发布以 lib/client.js 为准」警示。
- **dsh-raw-html 净化器加固**（模型可控 HTML 威胁模型）：`<template>` content 递归净化（嵌套模板覆盖）、SMIL animate/set/animateTransform 拦截（大小写双路径）、CSS url() 白名单（`#`/同源根相对/`data:image/`，其余→`url(about:blank)`）、外链强制 `target=_blank rel=noopener noreferrer`。sanitize 测试 14 项全过（jsdom）。

### 2.7 打包链（子智能体执行 + 我补 A1/A2）

- stage-resources：WebView2Loader 装配 win32-only；vendor/npm 缺失 fail-fast（提示 fetch-npm）；交叉打包护栏（target≠本机 platform/arch 即 throw）。
- make-portable：WebView2Loader.dll 存在则随包（与安装版产物面一致，best-effort）。
- installer-hooks.nsh：UninstallString 剥引号成对判定（`"path" args` 形态不再剥坏）；空 InstallLocation 跳过 ExecWait 只清注册表。
- release-macos.yml：版本号从 `dsh-desktop/package.json` 读取（原钉死 5.1.0）；release-tauri/clear-cache 头部 Electron 残留注释清理。
- **build-native.ts：RUSTFLAGS → `CARGO_ENCODED_RUSTFLAGS`**（linker 路径含空格必挂，两代交接文档虚报未落地的修复，本批真实落地）。
- **patch-deps.ts：7 处内核文件裸写 → writeFileAtomic**（同上虚报项）。
- 文档勘误：`docs/RELEASE-5.3.3-NOTES.md` 与 `docs/HANDOVER.md` 的虚报条目加 ⚠️ 勘误注记（「文档声称的修复必须对照 git 文件清单验证」教训入册）。
- 版本号 5.3.4 → **5.3.5**（package.json / tauri.conf.json / CHANGELOG 新条目）。

### 2.8 明确不修的（有据裁定）

- builtin-collision `stripPatchRows` 不加 isSelfWrittenRow 保护：有测试钉住「保护在 dupPreCheck 层」的 v4.4 设计，属已裁定语义非 bug。
- client-updater `MIN_VALID_BYTES=64MB` 下限：安全启发式，改动有放水风险，保持。
- psLnkRead/Write 同步阻塞（boot 期几秒）：异步化动静大，boot 期可容忍，已由 §1.3 的应答提前修复缓解（不再占应答路径）。
- phone-bridge 绑 0.0.0.0：LAN 桥设计使然（256-bit token/cookie 防护）。
- Rust `set_var` 在 async 线程（Windows 低危）、toast AUMID 注册缺失（需安装器实测）、monitor_from_point 主屏 scale 近似（推测性）：记录不动。
- dsh-whale-widget 1s 轮询 / dsh-dafeiyu 滑杆无防抖 / eac-locale-compat 全树走查：上游未改动件，备案不修。
- wizardMode 'first' 首启向导不可达：产品决策（first-run 分支是死代码），待用户裁定，未动。

---

## 3. 下一步行动清单（接手者按序执行）

> 全部命令在 Git Bash、仓库根 `D:\DeepSeek Harness\dsh max\dsh_desktop`。**注意 C 盘只剩 <1GB，任何 makensis/解压前先 `export TMPDIR=/d/dsh-tmp TMP='D:\dsh-tmp' TEMP='D:\dsh-tmp'`（目录已建）。**

1. ~~重出修复版安装包~~ **已完成**（16:33 产物已验收）。
2. ~~重出便携包~~ **已完成**（16:44 产物）。
3. ~~E2E 复验~~ **已完成：verify-phone-pair PASS（P1-P10 全过）**。若后续再动代码，复跑同款 + `verify-viewport-heal` + `boot-smoke`。
4. **给用户换包（下一步，交接者执行）**：请用户安装新 setup.exe（16:33 版）覆盖安装。用户 home 的 backups/（数 GB）会在新版本启动 30s 后后台异步清理（首次启动磁盘占用逐步下降属预期）；装好后点「连接手机→开始配对」应立即出二维码。
5. **全量回归收尾**（若 1-4 中改了任何代码）：`cd dsh-desktop && npm test`（当前基线 741/736/0，EXIT=0）。
6. ~~commit~~ **已完成：本地 commit `deb89cbc`（未 push，push 需用户明示）**。本文档更新部分若再改动，随手 amend 或补 commit。
7. **清理**（可选）：`dsh_desktop/tmp-rpc-probe/`、`tmp-phone-probe/`、根目录 `probe-*.js`、`rpc-probe-*.log`、`full-test*.log`、`tauri-build*.log` 均为本次排查产物；`tmp-icon-check/`、`tmp-viewport-heal/`、`tmp-ui-verify-installed/`、`tmp-p2boot/` 是历史调试目录（untracked，不阻塞）。

---

## 4. 验证状态汇总

| 验证 | 状态 |
|------|------|
| 全量单元/回归测试 | ✅ 741/736/0（EXIT=0，full-test5.log，含本批新增 14 用例：semver 5、atomic-write 4、cleanup 3、phone-bridge CSRF 1、raw-html sanitize 新增 4）|
| cargo check | ✅ 零警告零错误 |
| verify-viewport-heal（release 壳，坏版本前）| ✅ ALL PASS（T1-T4）|
| boot-smoke（隔离 home）| ✅ PASS |
| --bridge-test | ✅ 4/4 |
| verify-phone-pair | ✅ **修复版 PASS（P1-P10 全过，16:4x 复验）**；坏版本上的 context destroyed 不再复现 |
| 修复版安装包 | ✅ 16:33 产物（201,554,707B）|
| 修复版便携包 | ✅ 16:44 产物（240,826,039B）|

## 5. 环境陷阱速查（本机实测）

- **C 盘 120G 只剩 <1G**：makensis mmap / 任何解压前必须 TEMP 重定向到 `D:\dsh-tmp`。
- Cargo/工具链：`CARGO_HOME=D:\DSH\toolchain\.cargo`；tauri 2.11.5 源码在 `/d/DSH/toolchain/.cargo/registry/src/index.crates.io-*/tauri-2.11.5`（查 tauri 行为直接读它）。
- 编译链：`dsh-desktop` 的 tsconfig include 了 `../tauri-shell/sidecar/**/*.ts`，`npm run build`（pretest 自动跑）就地产出全部 .js（含 sidecar）；**noEmitOnError=true 是类型门禁**；staged-resources 由 `stage-resources.mjs` 从源树+编译产物装配，打包前必跑。
- 测试跑法：`node scripts/test-runner.js "test/xxx.test.ts"`（vendor/node 直跑 TS）；测试 import 编译产物 .js；`test/.dsh-home-repro/` 是夹具目录，grep 时排除。
- Git Bash 反斜杠：python heredoc 里的 `\\` 会被管道吃一层，正则/转义用 r"" 原始串。
- **绝不打开用户真实实例带 token 页面 / 不产生 API 花费**；冒烟一律隔离 `DSH_HOME`。
- 用户 home `<userData>/backups/` 有数 GB 历史镜像（新版本启动 30s 后异步清理，属预期）。

## 6. commit 规划（未 commit，工作区 46 改动 + 2 新文件）

建议拆分：
1. `fix(shell): 5.3.5 壳韧性+安全围栏+P0 配对挂死根治`（tauri-shell/* + dsh-desktop/lib/desktop/static-preview.ts + assets/ws-jsonrpc-client.js）
2. `fix(core): 更新器/原子写/logger/插件治理 18 项`（dsh-desktop 根层 + lib + scripts + 新测试）
3. `fix(plugins): 插件资产泄漏/契约/净化器加固`（assets/plugins/* + assets/*.html）
4. `chore(build/ci): 打包链护栏 + NSIS/macos CI + 文档勘误 + 5.3.5 CHANGELOG`（mjs/nsh/workflows/docs/CHANGELOG/版本号）

当前 HEAD：`deb89cbc`（5.3.5 批次，父 9c5c23ca=5.3.4）。push 必须等用户明示（EAC 规则）。

**已知新问题备案（本批 E2E 中观察到，未修）**：配对未完成时页面报 `window.onerror: cannot get property "remote.session" without inject` —— 内核/插件适配层问题（dsh-web 插件在未 boot 完成的 home 上访问 remote.session），与 P0 无关、不阻塞功能，留给下批评估。

## 7. 本批新认识（给下一个模型的提醒）

- **在 boot 关键路径上做任何同步文件操作前，先想用户 home 的数据规模**（我栽在 rmSync 几 GB 备份上）。
- **交接文档声称的修复必须对照 `git show <commit> --stat` 验证**——5.3.3 两代文档虚报了同一个修复两次。
- CDP 驱动真实壳时，「Execution context was destroyed」= 页面被导航（died 页弹出）——它本身就是诊断信号。
- 多副本纪律：settings-scroll-fix/phone-bridge/viewport-lock 有多份副本（assets↔staged↔target↔companion），改前先 grep 全部落点；stage-resources 会重新装配 assets+sidecar 编译产物，**改 .ts 后必须重跑 tsc（npm test 的 pretest 即可）再 stage**。
