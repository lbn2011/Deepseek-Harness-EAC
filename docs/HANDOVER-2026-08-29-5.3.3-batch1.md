# HANDOVER — 5.3.3 全库精简+Bug修复批次 · 第一批交接

> 交接时间：2026-08-29
> 交接人：ZCode（GLM-5.3-Flash）
> 接手人：下一个开发模型
> 仓库：`D:\DeepSeek Harness\dsh max\dsh_desktop`（main = `ef089a86`，已含本批全部改动；基线 5.3.2 = `00fefb02`；**未 push**——push 需用户明确要求，见记忆 dsh-eac-repo-no-push）

---

## ✅ 最终验证状态（本批收尾时实测，2026-08-29 深夜）

| 验证项 | 结果 |
|---|---|
| 全量 `npm test` | ✅ **717 通过 / 0 失败**，NPM_EXIT=0，总耗时 ~60s |
| `cargo test`（tauri-shell） | ✅ 3 passed（含新增 ws_handshake 准入测试 2 组） |
| 微信桥 `node --test test/bridge.test.mjs` | ✅ 52 checks passed |
| `npx tsc -p tsconfig.json --noEmit` | ✅ 绿 |
| 本地 commit | ✅ `ef089a86`（45 文件，+6130/−462） |
| push | ❌ 未做（等用户明确要求） |

### 交接文档写作后追加的测试修复（已包含在 ef089a86）
初轮全量跑出 9 个失败，全部是「行为变更对应的旧断言」，已按新语义更新测试：
1. `test/client-updater-proxy.test.ts`：2 个断言代理优先的测试 → 更新为直连优先（5.3.3 有意变更）
2. `test/diagnostics-zip.test.ts`：夹具 cordis.patch.yml 从 userData 挪到 dshHome 根 + mkdir userData + 断言入口名改 `config/profile/web-desktop/cordis.patch.yml`
3. `test/phone-bridge.test.ts`：8 处硬编码 `dsh_mobile=1` 改为从 pair-state 签发响应动态取回（`mobileCookieOf`/`approveAndCookie` 助手）；launch() 传临时 sessionFile（**根治了 undefined.tmp-\* 垃圾文件**）；新增「disconnect 后旧 cookie 401、重新配对新 cookie 放行」核心安全回归；10/10 全绿
4. `test/plugin-guard.test.ts`：3 个 guardedBoot 测试随死链删除（incident/snapshot 测试保留）
5. `test/recovery-center.test.ts`：clearStartFailure 断言更新为新语义（状态留给状态机、只清 lastError）
6. ⚠️ 排障记录：全量套件曾出现「三轮挂死」假象——根因是**两轮并发测试互相争抢资源 + 被杀进程留下僵尸子进程**，清场（杀全部 dsh 相关 node 进程）后单轮 60s 跑完。下个模型跑全量前先确认无残留 node 进程

---

## 0. 一句话现状

六路并行全库审查（约 90 条发现）已完成；**批次 A（8 项高危修复）、批次 E（快照回退链最小接线）、批次 B（约 30 项功能 bug 修复）、批次 C 的前 3 项性能优化已全部实施完毕并验证通过**。剩余工作：批次 C 余项、批次 D、批次 F、回归测试补齐、版本号、冒烟、打包、交付。

---

## 1. 背景与本批次的完整计划（用户已批准）

用户诉求：全局视角精简屎山代码 + 全面查找修复 bug，**不破坏原有功能、不引入新 bug**，最后全面测试+冒烟+打包验证。

审查发现汇总（六路）：
| 审查方向 | 范围 | 关键发现 |
|---|---|---|
| 宿主层 | dsh-desktop 根 19 个 .ts | guardedBoot 死链、logger archiver throw 崩进程、诊断 zip 路径错、agent-previous 备份永滞 |
| lib 层 | lib/ 33 个 .ts | EADDRINUSE 启动竞态、门控被架空、插件 Host 复活竞态、registerTool 键名错位、clearStartFailure 击穿状态机 |
| 前端/壳层 | sidecar + main.rs + assets | 回环 WS 无 Origin 校验（高危）、手机桥断开不失效（高危）、退出竞态孤儿进程、chrome.init 重读图标 |
| 微信桥 | openclaw-dsh-bridge | HTTP 状态不校验→回复静默丢失（高危）、二维码凭据发第三方（高危）、agent 泄漏 |
| scripts | scripts/ 39 个 | patch-deps l2 无守卫静默损坏、replace $ 模式陷阱、非原子写 node_modules、RUSTFLAGS 空格路径 |
| 测试/打包/卫生 | 180+ 文件 | docs/HANDOVER.md 严重过时、删除对话零单测、WebView2Loader 缺失不 fail-fast、README 引导下载退役 Electron 版 |

用户三项裁定（AskUserQuestion 确认）：
1. **死功能接线恢复**（static-preview / shortcuts 三函数 / junction-patrol / SessionWatcher）——不删除，接回壳层
2. **快照回退链最小接线**（markGood/reportIncident/confirmPrevious 接回；guardedBoot 完整重试链删除）
3. **微信桥纳入修复**

执行顺序：A→E→B→C→D→F→全量测试→打包→冒烟，每批独立 commit。版本 5.3.2→5.3.3。

---

## 2. 已完成：批次 A（8 项高危修复）✅

### A1. 壳层回环 WS 加 Origin/Host 校验（安全，高危）
- 文件：`tauri-shell/src/main.rs`
- 问题：`handle_conn` peek 后直接 `accept_async`，本机任意网页可跨站 WebSocket 连入 127.0.0.1:19873 调用全部壳层/侧车 RPC（files.revert 改文件、boot.stop 等）
- 修复：新增 `ws_handshake_allowed(head_lower)`——头部未完整终止（无 CRLFCRLF）拒绝；Origin 缺省放行；有 Origin 则 host 必须是 127.0.0.1/localhost/[::1]/tauri.localhost；Host 头同样校验（防 DNS rebinding）
- 测试：`ws_handshake_tests` 模块 2 组 8 断言（放行回环/tauri 源/无 Origin；拒绝跨站/rebinding/null 源/截断头）
- 验证：`cargo test` 3 passed ✅

### A2. 手机桥「断开并失效」真生效（安全，高危）
- 文件：`tauri-shell/sidecar/phone-bridge.ts` + `server.ts`（挂载点）
- 问题：cookie 是静态 `dsh_mobile=1`（一年有效），disconnect 只轮换配对 token，旧手机仍可全功能访问一年
- 修复：cookie 值改为服务端随机会话密钥 `sessionSecret`（32B randomBytes base64url），持久化到 `userData/phone-bridge-session.json`（跨重启保持配对有效，**手机端不需要重新配对**——这是行为保真的关键设计）；`disconnectAll()` = 轮换 secret + persist + rotatePairing；`hasPairedCookie` 用 `tokenEquals` 常量时间比对
- server.ts 挂载点新增 `sessionFile: path.join(userDataDir, 'phone-bridge-session.json')`
- ⚠️ 注意：`/desktop/disconnect` HTTP 路径与 RPC `disconnect()` 都走 disconnectAll

### A3. 启动清理门控恢复（issue #74 修复被架空，高危）
- 文件：`lib/desktop/companion-sync.ts` + `tauri-shell/sidecar/server.ts:352`
- 问题：sidecar preBootSync 直调**无门控版** `retireRemovedBuiltinPlugins`，随后 syncCompanionPlugins 里的门控版因已对齐而跳过——净效果=每次启动无条件删除性清理，同版本内用户手动恢复的插件树被强改
- 修复：`retireRemovedBuiltinPluginsGated` 改导出、无门控版收回内部函数；preBootSync 改调门控版
- 兼容：`test/retired-list-hash-gate.test.ts` 用 `src.indexOf('function retireRemovedBuiltinPluginsGated')` 取样——函数名未变，测试仍通过

### A4. logger archiver error 回调 throw 崩 sidecar（高危）
- 文件：`logger.ts` buildDiagnosticsZip
- 修复：archiver 'error' → 记 `archiveError` + `_state.onError(e)` + `output.destroy(e)`；finalize/finished 包 try-catch，失败删半成品 zip 后 rethrow
- 顺手修：诊断 zip 的 cordis.patch.yml 路径从 `userDataDir/profiles/web-desktop`（错根，永远静默缺文件）改为 `dshHome/profiles/{web-desktop,web}` 双 profile

### A5. boot-server 启动竞态 EADDRINUSE（高危）
- 文件：`lib/desktop/boot-server.ts` + `lib/desktop/proc.ts`
- 问题：Windows 控制台进程强杀在 killTree 内 +1500ms 落地；重入路径 kill 后 0ms spawn、受限端口换端口路径 600ms 后 spawn——均早于旧进程释放端口 →「启动失败」假阳性
- 修复：两条路径 spawn 前 `await waitForProcExit(old, 20000)`（现成 API killAndWaitForRestart 同款）
- 性能顺修（批次 C 提前完成）：`proc.ts waitForProcExit` 从 200ms execSync tasklist 轮询改为 `'exit'/'error'` 事件优先 + 500ms 轮询兜底（不再周期阻塞事件循环）

### A6. 插件 Host 三连（高危）
- `lib/extension-host/manager.ts`：init 握手失败 catch 补 `if (rt.stopping) return false` —— 否则 stopPlugin 在握手期间掐断 peer 会触发 startFailed→scheduleRestart，把用户刚停用的插件自动复活（heartbeatTick/onHostExit 都有 stopping 守卫，唯独此路径漏）
- `lib/extension-host/job-fence.ts`：JobFence.launch 与 FallbackFence.launch 补 `child.once('error', () => {})` —— spawn 异步 ENOENT 的 unhandled 'error' 会击穿 Supervisor
- `lib/desktop/market.ts`：
  - 超时强杀补平台分支（POSIX 走 child.kill('SIGKILL')，原 taskkill 无分支且无 error 监听——Linux 上 spawn ENOENT 异步抛无人接崩进程）
  - error/close 非互斥 → `settled` 标志 + `settle()` 统一收尾（防 attempts 双计、下一排队任务被跳过）
  - allowBuilds 自动重试分支也置 settled=true（让位重试）
  - `snapshot('market:' + job.label || job.target)` 优先级括号修复

### A7. 微信桥核心链路（高危，独立插件包）
- ⚠️ 关键背景：插件有**两份副本**——`dsh-desktop/assets/plugins/dsh-openclaw-bridge/`（内置分发，权威，含 rc.2 uSES 修复）与 `openclaw-dsh-bridge/`（dev 源）。5.3.2 时 dev 副本落后 222 行。本批已**互相同步到逐字节一致**（以 assets 为准，再叠加修改）。**下批改动必须两边同步**（wechat.js/client.js/index.js/openai-compat.js 四文件）
- `lib/wechat.js`：
  - `fetchJson` 补 HTTP 状态校验（`!resp.ok` throw）——token 24h 过期后 401/HTML 被静默当成功，回复链路无声丢失
  - `pollQrStatus` 加 `qrPollRunning` 单实例守卫；`submitVerify` 重启轮询（否则配对码无人消费卡 need-verifycode）
  - CHANNEL_VERSION 注释 131590→132102（实测编码）
- `lib/client.js`：
  - **本地二维码**：内联 qrcode-generator（MIT，从 `assets/plugins/dsh-phone/lib/qrcode.js` 提取 2289 行，`qrcode` 全局改名 `qrcodeGen`），新增 `qrDataUrl()` canvas 渲染；`qrImg` 不再拼 `https://api.qrserver.com/...`（凭据外泄+断网白屏）
  - `save()` 补 catch → `saveErr` state 上屏（红色错误 span）
  - ⚠️ 实施教训：我第一次注入时踩了 `String.replace(字符串, 替换串)` 的 `$` 替换模式坑（`case '$'` 被吞成 `case '`，语法损坏），git checkout 还原后改用**函数形式** `replace(anchor, () => injection + anchor)` 成功——这正是审查在 patch-deps.js 里指出的同类 bug，已在批次 B 修掉全部 7 处
- `lib/index.js`：
  - agent 池 `dropAgent(key)`（尽力 dispose）+ 池满 LRU 淘汰最久未用（原来直接 429 且旧 agent 永不回收）；`/new` 指令改走 dropAgent
  - 流式错误 delta 改通用文案「处理出错，请稍后重试。」（原 err.message 直发可能泄露内部路径），明细进 console.log
- 验证：`openclaw-dsh-bridge` 下 `node --test test/bridge.test.mjs` → **52 checks passed** ✅
- ⚠️ 环境注意：dev 副本跑测试需要 `openclaw-dsh-bridge/node_modules` junction → `../dsh-desktop/node_modules`（我已用 `cmd /c mklink /J` 建好，junction 已被 .gitignore 覆盖）

### A8. main.rs 退出竞态孤儿进程（中危）
- 文件：`tauri-shell/src/main.rs` ExitRequested 分支
- 问题：shutdown RPC 后固定 `sleep(500ms)` 就 kill sidecar，而 sidecar gracefulExit→stopServer 是 grace 1.2s + hard 4s（含 detached dsh web 树杀）——被拦腰砍断留孤儿 dsh web 占端口
- 修复：改为有界轮询 `owned.child.try_wait()`（200ms 间隔，上限 9s，覆盖 stopServer 上界+边距），超时才 kill
- ⚠️ 实施坑：`try_wait` 在 `Sidecar.child` 字段上不是 Sidecar 本身（第一次 cargo test 编译失败已修）

---

## 3. 已完成：批次 E（快照回退链最小接线）✅

背景：`guardedBoot` 全链路在 Tauri 化时断线（sidecar 从不调用）→ `markGood` 永不执行 → lastGood 恒空 → 恢复中心「回退最后良好快照」空转；`confirmPreviousAgentHealthy` 零调用 → 内核更新后数百 MB `agent-previous` 备份永滞。

- `lib/desktop/guard-box.ts`：GuardInstance 接口补 `markGood(id)` / `reportIncident(title, detail)` 两个声明（plugin-guard createGuard 返回面本就有）
- `tauri-shell/sidecar/server.ts`：
  - 新增 `guardedStartAndWait(overlays)`：snapshot('boot') → 成功 `markGood(snap.id)` + 首次健康启动 `confirmPreviousAgentHealthy(updCtx())`（`agentPreviousConfirmed` 模块级一次性标志）→ 失败 `reportIncident('boot-failed', ...)` 后 rethrow
  - **三条启动路径全部换用**：boot.start、restartWebServiceCore 未运行分支、重启分支
  - updater require 类型面补 `confirmPreviousAgentHealthy(c): boolean`
- `dsh-desktop/plugin-guard.ts`：删除 guardedBoot/rollbackPath/setRollbackLift/guardedBootRetryOnce 死链（79 行），保留 attributeBootFailure；返回对象与接口声明同步收敛
- `test/boot-attribution.test.ts`：删 3 个 guardedBoot 测试（保留 5 个 attributeBootFailure），头注释更新说明删除原因

---

## 4. 已完成：批次 B（约 30 项功能 bug）✅

按文件分组（全部已实施、tsc --noEmit 绿）：

**tauri-shell/sidecar/server.ts**
- restartingServer 死护栏接线：restartWebServiceCore 里 `vnextState.state.restartingServer = true/false` 与 boot-server setIsRestarting 同步（恢复中心 safeModeEnable 的 `running && !restartingServer` 护栏此前读恒 false 死字段）
- vnextState 类型面补 `restartingServer: boolean`
- installer 挂载 + preBootSync 接入 `sweepInstallerResidue()`（日志清扫计数）

**lib/extension-host/**
- `sdk/index.ts`：① registerTool 强制 `meta.name = 键名`（原来 `{...metaOrHandler, name: metaOrHandler.name || name}` 键名错位→注册成功调用必报 unknown tool）；② shell.exec 失败 `code: null`（原 ENOENT 字符串 code 落 0=成功）+ stderr 附加 `[spawn failed] ENOENT`；③ `withinRoots` win32 分支 toLowerCase 比较
- `rpc.ts`：dispatch notify 分支包 try/catch → stderr（原 onNotify 同步抛错=unhandledRejection→host-bootstrap exit(1)，EPIPE 就能把优雅退出变崩溃）
- `bridge-server.ts`：① `switch(req.url)` 精确匹配改 `new URL().pathname` 分流（带 query 即 404）；② close() 补 `closeAllConnections()`
- `host-bootstrap.ts`（根）：init 重入守卫 `initialized` 标志，二次 init throw

**lib/supervisor/**
- `registry.ts`：clearStartFailure 只清 lastError/lastErrorAt **不动 state**（原直接置 installed/disabled 会制造 quarantined→installed 非法转移，静默击穿隔离语义；且不清 crashStreak）
- `installer.ts`：① rollbackSdkPlugin 回滚点严格解析 `/^(.+)-(\d{13})$/` 且捕获组===id（原 `startsWith(id + '-')` 前缀碰撞会恢复**别的插件**的文件）；② 新增 `sweepInstallerResidue(keepRollbacksPerPlugin=3)`：.staging/.trash 全删、.rollback 每插件保留最近 3 份

**lib/desktop/**
- `client-update.ts`：runClientUpdateFlow 拆 wrapper+inner，busy 从入口即置位 finally 释放（原置位在弹窗/快照后，12h 周期与手动检查并发可双弹窗双下载）
- `companion-sync.ts`：隐私覆盖行幂等判定从精确正则改 `hasEntryId(patch, 'plugin-package-inventory-deepseek')`（用户重排 YAML 引号/注释即失配→追加第二条同 id 行）
- `plugin-ops.ts`：restoreCompanionPlugin 的 cordis.patch.yml 改 writeFileAtomic（原裸 writeFileSync 中断即截断→下次启动插件树校验失败）；import 自 `'../atomic-json'`（⚠️ 不是 './'，第一次写错已修）
- `feature-pack.ts`：① installPack/updatePack catch 补 `ctx.restoreSnapshot(snapshotRef)`（兑现文件头「失败按保护中心快照回滚」契约；snapshotRef 声明提到 try 外）；② runDshPlugin 补 5 分钟超时强杀（平台分支 taskkill/SIGKILL）+ clear timer
- `proc.ts`：waitForProcExit 事件化（见 A5）
- `market.ts`、`boot-server.ts`、`guard-box.ts`（见批次 A/E）

**dsh-desktop 根**
- `balance.ts`：fetchJson 总超时（setTimeout unconditional `req.destroy`）+ settled 一次性收尾（原 req.setTimeout 是 socket 空闲超时，慢速滴水响应永不触发→余额小部件永久转圈）；JSON.parse 失败路径也保证 settle
- `client-updater.ts`：① 代理改 `DSH_DESKTOP_GH_PROXY` 环境变量可配置（`0/off/false` 关闭），默认仍 gh.geekertao.top；② downloadUrls 直连优先、代理作候选（原代理优先于官方直连，域名易主拖垮全部下载）；③ 分片 .part 文件名掺版本 `${finalPath}.${release.version}.partN`（固定名跨版本断点续传拼包风险）+ 下载前清理历史版本遗留分片
- `logger.ts`（A4 已述）
- `rescue-agent.ts`：incidentBytes/totalBytes 改 `Buffer.byteLength`（原 string.length UTF-16 字符数，CJK 低估 3×）
- `profile-module-heal.ts`：shadow 目录 rmSync 单条 try（EBUSY/EPERM 不中断整个 heal）
- `preset-sync.ts`：mkdirSync 纳入 try（只读盘不再抛给 boot 链）
- `compact-preset-migrate.ts`：失败路径删 .tmp 残留；`temp` 声明提到 try 外（第一次作用域写错 tsc 抓出已修）
- `atomic-json.ts`：tmp 名加 `randomBytes(4)` 后缀（同毫秒并发写同一文件互相踩踏）
- `lib/recovery-center/register.ts`：safeModeEnable 写 cordis.patch.yml 改 `writeTextAtomic`（原裸 writeFileSync，torn write→下次 boot 全链失败）

**scripts/**
- `patch-deps.js`：① submenu 补丁 l2 二次 indexOf 改**偏移换算**（`labelIdx + (btnNew.length - btnAnchor.length)`，从根上消除 l2=-1 时 `slice(0,-1)` 砍掉 bundle 尾字符静默损坏）；② 7 处 `replace(字符串, 替换串)` 改函数形式（替换串含产物切片，`$&/$'/$\`` 会被解释为替换模式）；③ 新增 `writeAtomic()` 原子写替换 3 处 `fs.writeFileSync(target, src)`（postinstall 中断不再留截断的官方包）
- `patch-session-manage.js`：2 处 replace 函数形式 + writeAtomic
- `build-native.js`：`RUSTFLAGS` 改 `CARGO_ENCODED_RUSTFLAGS = "-C\x1flinker=..."` + delete RUSTFLAGS（原值不加引号，%TEMP% 含空格机器上 rustc 按空白拆碎 linker 路径；编码形式以 \x1f 分隔绕开）

**tauri-shell/**
- `stage-resources.mjs`：WebView2Loader.dll 缺失从 warn 改 **fail-fast `process.exit(1)`**（缺 loader 的安装包启动即 0xC0000135，绝不能让坏包流出炉）
- `src/main.rs`：① 新增 `encode_query()`（RFC3986 unreserved 之外全 %XX），encode_back 与 server-died log 参数共用（原只编码 `\ : / 空格`，`&`/`#` 截断参数）；② 主窗 `additional_browser_args` 从硬编码改追加用户 `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS`（对齐浮窗；原覆盖丢弃用户设置）
- `sidecar/phone-bridge.ts`：① 配对等待页 rejected/expired 终态 `clearInterval`（原永不停轮询）；② 代理 `res.on('close')` 未写完即销毁上游连接（手机端刷新不再堆积半开上游到 120s 超时）

---

## 5. 已完成：批次 C 前 3 项（性能）✅

1. **chrome.init 图标缓存**（server.ts）：模块级 `chromeIcon()`，图标只在首次 chrome.init 读一次 146KB+base64；bridge openMenu 每次开菜单不再重读+回环发 ~195KB
2. **waitForProcExit 事件化**（proc.ts）：见 A5
3. **file-roots mtime 增量缓存**（lib/desktop/file-roots.ts）：`cwdByFile Map<path, {mtimeMs, cwd}>`，缓存过期后只对 mtime 变化的 session.jsonl.zstd 重读+解压首帧（原来每次过期后 IPC 热路径全树同步重解，会话数百个时 UI 秒级卡顿）

---

## 6. 当前工作树状态（重要）

- ✅ **已全部 commit**：`ef089a86`（45 文件，+6130/−462），工作树干净
- `undefined.tmp-*` 垃圾问题的根因已根治：phone-bridge 测试的 launch() 现在传临时 sessionFile
- gitignore 提示：`verify-delete-session.js` 会写 `/tmp-verify-delete/`，仍未被 ignore（批次 F 项 3）
- 用户规则：本地 commit 随时可做，push 必须用户明确要求

---

## 7. 未完成工作（按优先级，接手指南）

### 7.1 批次 C 余项（性能，约 7 项）
1. **boot 期注册表 N+1 写合并**：`lib/recovery-center/register.ts:242` 附近 archivePluginProfiles 每插件一次 readRegistry+writeJsonAtomic（40 插件≈80 次 IO/40 次原子写，boot 热路径）。修法：一次 read、循环改内存、一次 write
2. **companion-sync 循环外读一次**：syncCompanionPlugins 内 34 个插件逐个 dupPreCheck 重读 profile package.json + cordis.patch.yml；pluginManagerPackageDescription 再逐插件读。修法：循环外读一次传参
3. **plugin-updater.ts 单次读 settings**：每个源/版本判断同步重读 settings.json；错误结果也进 10min TTL 缓存。修法：单次读取传入；错误不缓存或短 TTL
4. **dsh-web.log 启动截断**：boot-server.ts:135 `flags:'a'` 无轮转无截断，无限增长。修法：启动时 statSync >阈值（如 10MB）先截断（truncate 保尾部 1-2MB 亦可，rescue-agent 只读 tail）
5. **微信桥流式增量推进**：openclaw index.js `handleWechatMessage` 流式每 100ms 从 firstSeq 全量重扫 agent.session.events（长回合 O(n²)）；`lastAssistantText` 同样全量扫。修法：记录已推进 seq 下标增量
6. **bridge.ts popup rescue observer 节流**：tauri-shell/sidecar/bridge.ts:470 MutationObserver 挂 body subtree，每 mutation 批次同步 querySelectorAll——对话流式输出期间持续开销。修法：仿 settings-scroll-fix 加 rAF/150ms 节流
7. **childEnv/desktopProfile 记忆化**：proc.ts childEnv 每次调用 readFileSync+yaml.parse settings.yaml；profile.ts desktopProfile 每次 updater.loadSettings 无缓存。修法：按文件 mtime 记忆化或短 TTL
8. （已完成勿重做：chrome.init 缓存、waitForProcExit、file-roots 增量缓存）

### 7.2 批次 D：死功能接线恢复（用户已裁定：接线，不删除）
审查确认三个模块代码完整、有测试，但 Tauri 化时断线从未在新壳运行：
1. **static-preview**（lib/desktop/static-preview.ts）：`startPreviewStaticServer`/`getPreviewStaticPort`/`verifyBundledModules` 全零调用；sidecar 只 init 不启动（server.ts:66 附近 previewMod 挂载）。⚠️ 接线时 `isPackaged: () => false` 使 verify 恒早退——需先正确化。接好后 chrome.init 的 `staticPort: 0` 字段（server.ts chrome.init 返回值）才有真实值
2. **shortcuts**（lib/desktop/shortcuts.ts）：`maintainShortcuts`/`warnTempRun`/`migrateFromSharedWebProfile` 零调用；sidecar 挂载但只消费 applyLegacySkinChoice。⚠️ 一次性迁移 migrateFromSharedWebProfile 在 boot 序列早于 desktopProfile 确定时调用需谨慎
3. **junction-patrol**（lib/desktop/junction-patrol.ts）：`startJunctionWatchdog` 已 init 未启动；sidecar 用硬编码桩 ctx（`getServerProc: () => null`、`isRestartingServer: () => false`，server.ts:200 附近）。⚠️ **桩是雷**：直接按现桩启动，`detectExternalDsh` 会把桌面端自己的 dsh web 判为外部 dsh，修复永远被搁置——必须透传 boot-server 的真实 getServerProc/isRestartingServer；setInterval 无清理句柄要补；巡检 tick 的 execSync PowerShell（12s）要改异步（见 7.1 项 4 同类）
4. **SessionWatcher**（session-watcher.ts 类，非 scanZstdFrames）：2s 轮询引擎零消费者。用户裁定恢复，但先考古断线前行为（git 历史 Electron main.js 如何消费），经壳层通知通道（notify）恢复等价行为；若确认新壳无承载面，在交付说明明示不静默丢弃
每项独立 commit + 冒烟验证。连接线方案不确定时先读 `docs/adr/0002`（L1/L2/L3 架构对应）。

### 7.3 批次 F：死代码/卫生/文档
1. 删 5 个 test.skip 空壳（test/context-menu.test.ts、error-detail.test.ts、renderer-recovery.test.ts、session-encoding-heal.test.ts、watchdog-behavior.test.ts——每文件仅一行 skip，真测试已随 Electron 删除）
2. 归档 `upgrade-test-441.js` → `docs/archive/`（Electron 退役后不可再跑）；归档 `dsh-desktop/docs/MOBILE-CLIENT-DEV-SPEC.md`（5.1 自研客户端规格已被 5.2 反代取代）
3. `.gitignore` 补 `/tmp-verify-delete/`（verify-delete-session.js 运行产物）
4. `assets/plugins/dsh-settings-scroll-fix/` 删 src/ 或 lib/ 其一（双份完全相同双双进包）
5. onboarding.html：删 `.bar` 死样式（-webkit-app-region 在 WebView2 无效且 wizard 注入隐藏）；`<img src="icon.png">` 在 /wizard 路由 404（改内联 data URI）
6. exit-overlay.js:58 show() 每次累加 keydown 监听只有 Esc 路径自移除——show 前移除旧引用
7. 删 `tauri-shell/sidecar/ping.ts`（零消费者，仅注释提及）
8. plugin-guard.ts:318 listFallbackNames 无效果分支 + 363-380 fallbackPackages 重复枚举合一
9. `scripts/analyze-session-log.js` 调试切片 + `repair-session-log.js` 重复 ~55 行 scanZstdFrames 抽 `scripts/lib/zstd-frames.js`
10. `feature-pack-cli.ts/.js` 行数不一致（存在未重新 build 的提交窗口）——建议把 `scripts/**/*.js` 加入 .gitignore（与主应用 .js 同策略，tsc 编译产物）
11. README.md:70 下载表仍指 v4.4.1 Electron → 改 5.3.3 Tauri 资产；README.md:225 Electron 折叠块里是 Tauri 构建链 → 改 summary
12. **docs/HANDOVER.md 严重过时**（停在 2026-08-22，称 Tauri 是 PoC、引用已删的 main.js）——重写或归档；本文件（HANDOVER-2026-08-29-5.3.3-batch1.md）可作为新交接的基础
13. CI 注释 3 处：release-tauri.yml:2、clear-cache.yml:2-3（electron 残留注释）、release-macos.yml:31（input 默认值 macos-arm64-5.1.0 过时）
14. `dsh-desktop/scripts/test-watcher.js:2` 头注释与用途不符
15. `junction-patrol.ts:33` 桩 ctx 的雷（即使批次 D 不接线也应在代码注释标注）
16. `settings-scroll-fix` 插件 src/lib 双份（见 4）
17. 小件：`runtime-patches.ts:28` runtimePatchRoots 死导出、`incidents.ts:62` listIncidents 死导出、`registry.ts:102` recordStartFailure 死代码、`rpc.ts isClosed()` 死代码、`lib/state.ts` serverProc/forceQuit 死字段、`job-fence.ts:45` sha256Stream 死接口、`feature-pack.ts:472-475` 空 if 体、`plugin-guard.ts:12` Electron 注释、`dsh-web-mobile-fix` 哈希选择器迁 data-* 锚（中风险）、`installer.ts:56-62` rmrf 注释「留给下次清扫」已兑现可更新注释
18. 已知遗留问题（审查发现、用户未裁定、本批未动）：`updater.ts:37,143` activeProc 全局变量并发踩踏（abort 已死代码暂无实害）、`updater.ts:207` stdoutBuf 无上限、`logger.ts:819` 诊断导出同步读 10×20MB 内存尖峰、`sdk settings.get` 每次全量重读、`job-fence.ts:89` 原生模块路径硬编码、`companion-sync.ts:689` ensurePluginHostDeps 声明位置靠提升、`market.ts:126` marker 非原子、`openclaw wechat.js:192` submitVerify 后 pollQrStatus 若 session 未变仍用旧 qrcode（已修重启轮询但 qrcode 过期场景未测）、`openclaw authorized()` 非常量时间比较、`openclaw scripts/*.ps1` 硬编码机器路径（install.ps1:19 D:\app\dsh、integration-test.ps1:8 C:\Users\delinger、test.ps1:17 USERPROFILE 污染不还原）、`uninstall.ps1:31-32` 重复行、`integration-test.ps1:23` ASCII 编码写中文 body、`verify-viewport-lock.cjs` 依赖全局 playwright

### 7.4 回归测试补齐（+15~25 用例目标）
审查确认的盲区（按价值排序）：
1. **deleteSession/unarchiveSession 零单测**（5.3.2 主打修复只有手动冒烟 verify-delete-session.js）——把冒烟断言抽成 node:test 集成测试
2. **healCredentialsVersion 零单测**（boot-server.ts，凭据版式自愈是「升级后启动卡死」级功能）——纯本地可测：引号 version、rc.2 扁平格式、正常 version:1 不动
3. **atomic-json 并发**：同毫秒并发写不互相踩踏（tmp 随机后缀回归）
4. **手机桥 cookie 失效**：disconnect 后旧 cookie 401、新配对后新 cookie 放行、重启后（模拟 sessionFile 保留）旧 cookie 仍放行——phone-bridge.ts createPhoneBridge 已可注入 sessionFile 临时目录直测
5. **manager stopping**：init 握手期间 stopPlugin 不复活
6. **registerTool meta.name**：三参注册后 invoke 成功
7. **installer rollback 严格解析**：id 'foo' 不匹配 'foo-bar-...' 回滚点
8. **sweepInstallerResidue**：.staging/.trash 清、.rollback 保 3
9. **client-updater part 掺版本**：5.3.2 旧式 part 文件被清理
10. **guardedStartAndWait**：成功 markGood 落盘、失败 reportIncident 落盘（可对 guard 实例 mock 或临时 home）
11. **ws_handshake_allowed**（已有 8 断言在 main.rs，保持）
12. **微信桥**：fetchJson 非 2xx throw（52 checks 里可能缺此新行为断言，补一条）

### 7.5 版本与交付
1. `dsh-desktop/package.json` version → **5.3.3**（⚠️ 记忆教训：版本必须变号，profile 同步白名单才认）
2. 全量 `npm test` + `cargo test` + 微信桥 `node --test test/bridge.test.mjs`
3. 冒烟：boot-smoke.js、gui-smoke.js（真实壳 CDP）、update-smoke.js、verify-delete-session.js、verify-phone-pair.cjs、ui-verify-smoke.js、rescue-smoke.js（均在仓库根）
4. 打包链：`cd tauri-shell && node stage-resources.mjs && npx @tauri-apps/cli build && node make-portable.mjs`（⚠️ 记忆坑：打包须 npx @tauri-apps/cli；makensis mmap C 盘满需 TEMP 重定向；robocopy 转义；便携包路径反斜杠；stage 现在会因 WebView2Loader 缺失 fail-fast——这是预期行为，先跑一次 tauri build 让 cargo 拉包）
5. NSIS 安装态冒烟 + 便携包启动冒烟
6. 交付物 `release-5.3.3/`（setup + portable + 交付说明）

---

## 8. 验证状态快照（交接时刻）

见文首「✅ 最终验证状态」——**全部绿**：npm test 717/0、cargo 3 绿、微信桥 52 测全绿、tsc 绿、已 commit（ef089a86）。§9 的 commit 方案已执行完毕（采用方案甲单 commit）。

---

## 10. 关键避坑清单（本批实测踩过/审查确认）

1. **String.replace 第二参数函数形式**：替换串含 `$`（如产物切片、含 `case '$'` 的代码）必须用 `() => repl` 形式——本批 client.js 注入实翻车一次（git checkout 救回）
2. **tsc 编译产物 .js 与 .ts 同目录**：改 .ts 后必须 `npm run build`（test 的 pretest 会自动 build）；直接跑单测也要先 build
3. **微信桥双副本**：assets（权威）与 openclaw-dsh-bridge 必须同步，改完 `cmp` 校验
4. **openclaw 测试需要 junction**：node_modules → ../dsh-desktop/node_modules（已建）
5. **stage-resources WebView2Loader 现在 fail-fast**：打包前先确保 cargo registry 有 webview2-com-sys
6. **cargo 编译错误先看字段归属**：Sidecar 包装结构，方法在 .child 上
7. **版本变号才触发 profile 同步**：5.3.3 bump 是功能依赖不是仪式
8. **不碰 vendor/kernel/** 与 assets/plugins/** 第三方插件**（dsh-openclaw-bridge/dsh-phone 等自有插件副本可改）
9. **测试环境必须无 key 隔离**（记忆 dsh-no-api-spend）：绝不打开用户真实实例带 token 页面
10. **push 必须用户明确要求**

---

## 11. 参考文件索引

- 本批改动全量：`git status` / `git diff`（38 文件）
- 架构：`docs/adr/0002`（L1 Rust 壳 / L2 Node sidecar / L3 内核零改动）
- 前一批次交接：`docs/archive/HANDOVER-*.md`（8 轮归档）
- 内核升级注记：`docs/KERNEL-UPGRADE-012-NOTES.md`
- 旧权威交接（已过时勿信正文）：`docs/HANDOVER.md`
- 冒烟脚本说明：各脚本头注释（boot-smoke/gui-smoke/update-smoke/verify-*）
