# 2026-08-28

## CI 89662976410：clippy 绿，Node 测试 2 失败（commit 6214822）

- 上轮 clippy 修复生效（13 步过）。Node 测试两平台各 2 失败：
  1) **Task 10.5 Electron 扫描**：main.rs 358 行注释「与 Electron 壳共用一份」
     —— 本地之前全量失败也是它（当时误判并行干扰，实际是真问题）。
     修复：改「与桌壳共用一份」，受检路径（package.json+lib+tauri-shell）
     electron 扫描清零。
  2) **注册表残留**：third-party-thinking -> dsh-third-party-thinking 指向
     不存在资产（上游已删插件目录，我方注册表残留）——删条目。
- 验证：本地全量 843 过 837（唯一失败 safe-delete shim artifact，CI 无）。
- 教训：**Task 10.5 是 git 内容扫描（不只 Electron 版文件）**——任何代码/注释
  里的 "electron" 字样都挂；本地全量失败别急着归因并行干扰，先看是不是真问题。

## CI 89778498502：全链路首次触达终点——仅 linux-smoke 卡（commit 9730d23）

- **里程碑**：两平台 tauri build + 产物上传全部成功！Windows NSIS 179MB（256MB 上限过）、Ubuntu deb+AppImage（378MB 产物）。Node 测试全绿（上轮修复生效）。
- 最后一步 linux-smoke 容器冒烟失败：`chmod ...AppImage: Read-only file system` —— docker 挂载 artifacts 为 :ro，脚本 cd 进挂载目录 chmod +x。修复：脚本开头复制产物到 mktemp 工作目录（/tmp/dsh-smoke.XXXXXX）。
- 教训：**容器挂载 :ro 时脚本所有写操作（chmod/解包）必须先在可写目录复制**。

## CI 89794435407：主流水线全绿，linux-smoke L2 deb 依赖缺失（commit b7642dd）

- **主流水线（Tauri ubuntu/windows）全绿**（clippy/cargo/Node/build/上传）。
- linux-smoke：L1 AppImage 提取过（上轮只读修复生效）→ **L2 deb 安装失败**：
  `deepseek-harness-eac depends on libgtk-3-0; not installed`——冒烟容器只装
  libfuse2 ca-certificates，缺 Tauri v2 deb Depends 链。修复：apt 补
  libgtk-3-0 libwebkit2gtk-4.1-0 libayatana-appindicator3-1 librsvg2-2。
- tauri.conf.json 无自定义 deb depends（用 tauri 默认）。

## CI 89803924161：L2 deb 布局问题 + 用户要求扩展冒烟（commit d7389a9）

- L1 AppImage 过；L2 deb 安装「缺关键路径」——dpkg -i 输出被 >/dev/null 吞（8s 装完但 grep dpkg -L 失败）。改脚本显示 dpkg 输出定位（待下一轮 CI）。
- **用户要求**：1) 增加 Windows 冒烟；2) 冒烟增加启动测试且要求**进入对话界面**。
- 实现：
  - sidecar-boot-probe.js 增强：boot.start → HTTP 首页 200 + body 校验（>512B + </html> + chat/conversation/#app 标记）+ 打印首页摘要。Linux L3 与 Windows S3 共用。
  - ci.yml + windows-smoke.sh：NSIS /S 静默安装 → 安装树断言（exe/sidecar/dsh-desktop/vendored node）→ vendored node 直启探活 → NSIS 卸载。
- 经验：Windows 产物 zip 只有 NSIS exe（安装后从 LOCALAPPDATA 找 dsh-eac-shell.exe）；对话界面验证用 HTTP 首页标记（GUI 截图太重）。

## Linux 启动测试明确化（commit 376e785）

- 用户要求"linux也要有启动测试"。Linux 冒烟 L3 本就用增强探活（与 Windows S3 同一 sidecar-boot-probe.js），明确步骤名「L3 启动测试（进入对话界面）」+ 注释。容器 headless 无 X，sidecar 直启 + HTTP 对话界面验证 = GUI 启动服务端等价。

## 重大修复：打包态 sidecar 路径解析 bug（commit 53338b1）+ smoke 修复（0b242fb）

- **用户实测**：CI 版安装（LOCALAPPDATA，与 Program Files Electron 老版并存）启动
  「DSH 服务已停止 / sidecar call timeout (180s)」。手动复现：安装树
  server.js require('../../dsh-desktop/lib/state.js') MODULE_NOT_FOUND。
- **根因**：`../../` 从 <install>/sidecar 解析 = <install 上级>/dsh-desktop，任何
  「sidecar 与 dsh-desktop 同级」布局都不匹配；开发态（tauri-shell/sidecar →
  仓库根）碰巧对。**Tauri 打包以来一直潜伏**（L3 探活从未跑通过；Windows smoke
  首跑暴露）。
- 修复：server.ts 26 个业务 import → DSH_DESKTOP_ROOT 动态 require（load helper）；
  ipc-surface/ping/rescue-integration 的 __dirname 解析加 upOne fallback。
- 验证（安装树实测）：vendored node 直启 → boot 13.8s → HTTP 200/22KB/chat-marker ✓
- CI 修复：L2 SIGPIPE（grep -q→-E）误判缺路径；Windows S1 NSIS 挂起加 timeout。
- **用户机器已手动替换安装树 sidecar（server/ipc-surface/rescue-integration/ping）
  可立即重启验证**；正式修复随下轮 CI 构建。

