# 2026-08-27

## CI 日志 89481668262 定位并修复 main.rs 编译错误（commit 063d355）

- 新 CI run（用户推送 2c89a69 合并树后触发）：**Linux fetch-runtime 已过**（fetch-npm 修复生效，命中 vendor/npm），两平台其余步骤全绿，唯一失败 = 步骤 13 `Tauri 壳测试`。
- 失败根因（真实 Rust 编译错误，非环境）：`tauri-shell/src/main.rs:99` 报 `unexpected closing delimiter: }`——合并上游 dba99e3b 时，上游 `resource_root()` 的闭合 `}` 与我方 184cc716 新增的同名函数叠在一起，多出一个 `}`。两平台同一报错（ubuntu: main.rs:99:1 / windows: main.rs:99:1）。
- 这是**占位目录修复（69282ba）后的下一层**：之前 cargo test 卡在 build script（staged-resources 校验），编译没走到 main.rs；占位后前进到真正编译，暴露语法错误。
- 修复：删除多余 `}`。验证：`rustc --edition 2021 --emit=metadata src/main.rs` 无 parse 错误（144 error 全为裸 rustc 缺依赖的 E0432/E0433/E0308 级联）；本地 cargo check 仍被 MSVC lib.exe 0xC0000139 阻断（CI 无此问题，仅本地环境限制）。
- 教训：**合并后必须验证 Rust 侧编译**——git 合并叠代码块会产生重复闭括号这类 parse 错误，typecheck（TS）覆盖不到。

## CI 日志 89501515165 定位并修复 nav_fence IPv6 bug（commit 0317e2c）

- 上轮 main.rs:99 修复后，cargo test 编译通过（Finished test profile in 1m43s），6 个 nav_fence 测试 5 过 1 挂：`allows_loopback_host_on_ws_port` 断言 `allowed("http://[::1]:19873/")` 失败（两平台同一测试）。
- 根因（本地实测 url crate 2.5.8）：`host_str()` 对 IPv6 返回**带方括号** `"[::1]"`（IPv4 返回 "127.0.0.1" 不带括号），原 `matches!(host_str(), Some("::1"))` 永远不匹配。
- 修复：改用 `url::Host` 类型化匹配（`Host::Ipv4/Ipv6` + `is_loopback()` + localhost 域名），Cargo.toml 加 `url = "2"` 直接依赖（lock 已锁 2.5.8）；顺手加 `[features] devtools = []` 消除 main.rs:445 unexpected_cfgs 警告。
- 本地验证突破：**用 rust-lld 作 linker 可绕开坏掉的 MSVC link.exe/lib.exe**（`RUSTFLAGS="-C linker=<repo>/tauri-shell/target/lld-link.exe"`），纯 Rust crate（url 等无 cc 依赖）可本地编译运行——urltest 复现修复逻辑 6/6 通过。tauri 依赖链（vswhom-sys build script 调 lib.exe）仍无法本地编译。
- CI 链路全景（ci.yml）：npm ci → fetch-runtime → typecheck → clippy → test:native → build TS → cargo test(当前) → npm test → build:native → tauri:build → NSIS 体积断言 → 上传 → linux-smoke 容器冒烟。tauri:build 与 linux-smoke 是剩余最大未知区。
- 教训：**合并产生的 bug 是一层层暴露的**——build script 校验 → 语法错误 → 运行时逻辑测试。每修一层下一层才可见；本地用 rust-lld 能验证纯 Rust 逻辑，但 tauri 依赖链编译只能靠 CI。

## CI 门禁强化（commit af0de9b）——clippy + 单测 + lints

- **Tauri 壳 clippy**：ci.yml + release-tauri.yml 在 cargo test 前加 `cargo clippy --all-targets -- -D warnings`（此前 tauri-shell 完全没被 lint，只有原生模块 clippy:native）。
- **cargo test --no-fail-fast**：语义明确。
- **main.rs 12 个纯函数单测**：sanitize_label(4) / is_safe_external_url(4，安全关键) / dsh_desktop_dir(1) / resolve_node env 覆盖(1) / is_allowed_main_navigation 集成(2，None 路径)。修正了 sanitize_label 对全非法字符（"!!!"→"---"）不回退的语义断言（初始写错成 "float"）。
- **Cargo.toml [lints.rust] unexpected_cfgs = "deny"**：本 crate 级，未声明 feature 的 cfg 直接编译失败。
- 验证：main.rs --cfg test parse 无语法错误；新断言 urltest 复现 8/8；YAML 两 workflow 校验 OK；cargo metadata 含 lints 解析 OK。
- 可复用模式：**本地 rust-lld 绕坏 MSVC 链接器跑纯 Rust 验证**（RUSTFLAGS="-C linker=<repo>/tauri-shell/target/lld-link.exe"）；CI 无法本地复现的只剩 tauri 依赖链编译。

## CI 89504249869：新 clippy 门禁首跑暴露 10 处 lint（commit ~af0de9b 后）

- 用户推送 af0de9b（Tauri 壳 clippy 门禁）后，clippy -D warnings 首跑即红：Ubuntu 8 处 / Windows 9 处（cfg 分支差异），全部来自上游合并代码（此前 tauri-shell 从没被 lint 过）。**这正是加门禁的价值——一次暴露一批**。
- 修复 10 处：88 unused_import（configure_command 的 Command 是 tokio::process::Command 自带 creation_flags，std CommandExt 多余；run_clipboard_command 的 as_std_mut 才需要 → 模块级 #[cfg(windows)] use 共用）、160/1224 type_complexity（PendingRpc/BridgeCheck 别名）、449/451/1692 let_unit_value、698/715 needless_return（cfg 分支尾改尾表达式）、854 io_other_error、1669 collapsible_if（match guard）。
- 验证：rustc --cfg test parse 无语法错误。本地仍无法跑 tauri-shell clippy（vswhom-sys build script 调坏掉的 lib.exe）；CI 是唯一完整验证。
- 教训：**tauri-shell 本地 lint 盲区**——clippy 依赖 tauri 链编译，本地 MSVC 坏。rustc parse 只能查语法，查不了 lint。给新门禁的代码要格外小心。

## CI 89529967473：clippy/cargo test 全绿，Node 测试 retry 目录 bug（commit 修复）

- **重大进展**：Tauri 壳 clippy（13）过、cargo test（14）18 个测试全过（nav_fence 6 + 新单测 12）。上轮 10 处 lint 修复 + nav_fence IPv6 + main.rs 语法全部验证通过。
- 失败前进到 Node 测试（15，retry action）：两平台 `npm error ENOENT .../Deepseek-Harness-EAC/package.json`。
- 根因：**nick-fields/retry 是 uses action，不继承 defaults.run.working-directory**（只对 run 步骤生效），其内部 shell 在仓库根跑 npm test。此 bug 一直存在，之前步骤挂了没走到。修复：command 加 `cd dsh-desktop &&`（shell 默认 bash 跨平台）；release-tauri.yml 的 Node 测试是普通 run 继承目录无需改。
- 教训：**uses 步骤（action）不继承 working-directory**——凡在 action 内跑 npm 命令都要显式 cd。
