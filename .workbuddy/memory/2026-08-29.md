# 2026-08-29

## 项目 Bug 审查（Deepseek-Harness-EAC）
- 用「软件开发团队」专家走部分工作流：组建 `software-dsh-audit` 团队，工程师（寇豆码）做 5 区块静态审查，QA（严过关）测试资产线因平台限流（429，约 23h 后才重置）两次失败未交付。
- 成果：累计 **87 条缺陷**（A14/B28/C20/D15/E9/F1），**P0×3 / P1×24 / P2×39 / P3×21**。
- 3 条 P0：
  1. **E-01** 回环 WS 桥 `127.0.0.1:19873` 无 Origin 校验、token 客户端自声明 → 任意网页可接管主窗（主理人已逐跳核对源码确认）。
  2. **C-01** 更新失败回滚 `robocopy /MIR` 删用户 sessions（备份排除 sessions）。
  3. **C-02** `DSH_DESKTOP_RELEASE_API` 可劫持更新源 + 哈希同源，无独立可信锚点。
- 两条跨区块主线：①「安全设计」注释与实现脱节（B-10/A-04/C-05/B-04/E-03）；②持久化写入原子性不统一（B-07 等 6 处非原子，建议抽 `writeFileAtomic`）。
- 主理人补审 `tauri.conf.json`：`csp:null` 且项目无 capabilities 目录，Tauri ACL 纵深防御实为摆设（新增 F-01）。
- 报告落盘：`.workbuddy/deliverables/bug-audit-2026-08-29.md`。
- 未完成：QA 测试资产线、区块 F 剩余文件（phone-bridge/ping/rescue-integration/snapshot-ui、各 .mjs 脚本、windows-smoke.sh、redact.ts 覆盖度）——限流恢复后建议补审。

## 全项目 Bug 大排查(2026-08-29)
- 6 个并行审计代理分区逐文件审查(根脚本/tauri Rust+sidecar/dsh 根模块/lib 顶层/lib 子目录+shared/scripts+test+native),发现即写入 `.workbuddy/bug-fragments/*.md`,最终合并为 `bug-report.md`(V1.0)。
- 合计 94 条:确认 70、待复查 24;S 级 7(确认 5)。
- 关键确认项:phone-bridge 静态 cookie 认证绕过(B-010)、server-lock 锁文件无 wx 竞态(D-001)、client-update concatFiles 与 diagnostics archive 错误处理崩溃(E-001/E-003)、native snapshot store.rs 路径穿越(F-011)、sidecar server.ts 未注册 IPC surface(B-008)、feature-pack 漏 await/不回滚(D-004/005)、updater.ts require 缓存+settings.json 非原子写(C-012/013)。
- 系统模式:http timeout 语义误用(5 处)、进程断言恒真(3 处)、e2e setTimeout+exit 清理失效(4 处)、非原子写配置多处、测试 `|| true` 死断言多处。
- 注意:tsconfig exclude 了 shortcut-maintenance.ts 与 stream-write-guard.ts(根级前者为孤儿文件,lib 下后者才是运行版)。
