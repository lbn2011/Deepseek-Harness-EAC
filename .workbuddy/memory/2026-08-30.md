# 2026-08-30

## 第二轮 bug 排查：对比 upsterm 上游（bug-report.md → V1.1.1，94→113 条）

- **分叉关系（关键，易错）**：merge-base=671e87ec，本地领先 123 / 落后上游 32 提交。第一次排查误判为"upsterm 是严格祖先"，被子代理纠正。教训：fetch 后必须用 `git rev-list --count` 双向核对再下结论。
- **G 区（本地回归，12 条）**要点：
  - G-001(S 确认)：本地删了 tauri.windows.conf.json → WebView2Loader.dll 不进安装包 → 安装版 0xC0000135（坐实 A-017；开发态 cargo run 不受影响，webview2-com 会把 dll 放到 target/）。
  - G-105(M)：托盘菜单打不开根因=tray-icon 默认 menu_on_left_click:true 与左键 Click 处理器 set_focus() 互搏；修复=`.menu_on_left_click(false)`。
  - G-106(M)：/died 重建窗没挂 initialization_script → retry 成功导航 webUrl 后桥永失（菜单按钮消失+窗口不可拖动关闭）；一行修复。
  - G-002：server.ts 删了 file-drop.save 处理器但 bridge.ts 还在调 → 文件拖入失效。
  - G-101：menu.action export-logs 调不存在的方法名 recovery.export-logs（真实是 chrome:export-logs），独立于 B-008 的第二断点。
  - G-102：update-flow.ts installDir=dirname(process.execPath)，sidecar 下是 node 目录而非壳 exe → 客户端更新写错目录。
  - G-003：窗口状态恢复逻辑/物理像素单位混用（min_vis 用逻辑宽度 clamp 物理坐标；中心点用主屏 scale 选显示器）。
- **H 区（未合并上游修复，7 条待复查）**：最重要的是 H-002（上游 25a8ccc/5d89422 凭据库修复=升级后启动卡死元凶，S 级待对照）；b0a593a --no-open 本地已有等效实现（server.ts:167），已排除。
- 手机桥：本地 5.1.1 旧版带 B-010 静态 cookie 认证绕过；上游 5.2.0(f45f25b) 已整体重写为反向代理——B-010 修复路线建议优先评估合并上游。

## 合并评估 + 批量修复（bug-report.md → V1.2，22 修 1 部分修）

- **合并评估结论**：`git merge-tree` 试算 48 个冲突文件，15+ modify/delete 结构性冲突（本地"统一 lib 模块族"重构删了 lib/desktop/*，而上游热修全落在那里）——**不建议直接合并**，走上游修复定点移植路线。
- **已修 22 条**（关键）：B-008 IPC surface 注册（+主窗注入 __DSH_BRIDGE_SESSION__ 配套 sender 绑定，冒烟实证三通道通）、G-001 恢复 tauri.windows.conf.json、G-105 tray .menu_on_left_click(false)、G-106 /died 窗补 initialization_script、G-003 WindowState 加 scale 字段修单位混用、G-002 fileDropSave 移植回 plugin-manager-core、H-002 healCredentialsVersion 移植进 lib/server.ts（startServer 入口调用）、B-010 手机桥随机会话 cookie、F-011/F-012 native store 白名单+边界、E-003/C-012/C-013/B-002/B-004/B-005/B-011/C-008/G-004/G-101/G-103/G-104。
- **本机环境坑（重要）**：① Windows 受控文件夹访问 (CFA) 拦截 cargo.exe 写 Desktop 下 target → .cargo-build-lock os error 5，须设 CARGO_TARGET_DIR 到 %LOCALAPPDATA%\Temp 绕开；② 本机 VS 18 BuildTools 的 link.exe 启动即崩 0xC0000139（入口点找不到，VsDevCmd 环境同样崩）→ cargo check/test 全废，Rust 改动只能用 rustfmt 解析验证，需用户修复 VS Build Tools 后补验；③ Node 测试要 26+（用 C:\Program Files\nodejs\node.exe，managed 22 不行）。
- tsc 全量（含 sidecar）类型检查+编译通过；sidecar stdio 冒烟实证 B-008 修复。
